// 修复后的登录逻辑 - 移除硬编码密码
// 文件: src/controllers/auth.controller.ts

// 原代码（有问题的）:
/*
    // 如果是管理员账号且不存在，自动创建
    if (!user && email === 'admin@aafa.com' && password === 'admin123456') {
      const hashedPassword = await bcrypt.hash('admin123456', 10);
      user = await prisma.user.create({
        data: {
          email: 'admin@aafa.com',
          password: hashedPassword,
          name: 'AAFA管理员',
          role: 'ADMIN',
        },
      });
    }
*/

// 修复后的代码:
    // 管理员自动创建逻辑已移除
    // 请使用环境变量配置初始管理员或手动创建
    // 创建管理员命令: npx prisma db seed 或使用注册接口后改 role 为 ADMIN
